@model List<Employee>

@if (Model.Count != 0)
{
    <h3 class="mb-4">افراد کارجو</h3>


    <div class="container">
        <table class="table">
            <thead>
                <tr>

                    <th scope="col">نام</th>
                    <th scope="col">عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td>@item.Name @item.LastName</td>
                        <td>
                            <a class="btn btn-success" asp-action="GetFormById" asp-route-id="@item.Id">مشاهده</a> | <a id="btndelete" class="btn btn-danger"  asp-action="DeleteForm" asp-route-formId="@item.Id">حذف</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>

        <pre></pre>


        <a href="/" class="btn btn-primary">بازگشت</a>


    </div>
}

else
{
    <div class="alert alert-danger">
        فرمی برای نمایش وجود ندارد
    </div>
}





@*<div id="mymodal" class="modal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">حذف کارجو</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>آیا از حذف کارجو مطمئنید؟</p>
            </div>
            <div class="modal-footer">
                <button  type="button" class="btn btn-success" data-bs-dismiss="modal">خیر</button>
                <button asp-action="DeleteForm" asp-controller="Form" asp-route-formId="" type="button" class="btn btn-danger">بله</button>
            </div>
        </div>
    </div>
</div>


@section Scripts{

<script>

        $('#btndelete').click(function () {
            $('#mymodal').modal('show');
        });
    
</script>
}*@