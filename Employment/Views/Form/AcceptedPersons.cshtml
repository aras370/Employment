@model List<AcceptedEmployee>

   

@if (Model.Count != 0)
{
    <h3 class="mb-4">پذیرفته شده ها</h3>

    <div class="container">
        <table class="table">
            <thead>
                <tr>

                    <th scope="col">نام</th>
                    <th scope="col">عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td>@item.Name @item.LastName</td>
                        <td>
                            <a class="btn btn-success" asp-action="AcceptedPerson" asp-route-id="@item.Id">مشاهده</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
        <pre></pre>


        <a href="/" class="btn btn-primary">بازگشت</a>


    </div>
}

else
{
    <div class="alert alert-danger">
        فرمی برای نمایش وجود ندارد
    </div>
}